name: gradientbox
description: A package that lets you add a beautiful modifiable Gradient Box to your Flutter app.
version: 0.0.1
homepage: https://github.com/paanTom/gradientbox

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
